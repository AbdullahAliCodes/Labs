#include <iostream>

using namespace std;

int x;

int minVal = 9999;
int main() {
    cin >> x;
    while(x != -1){
        if(x < minVal){
            minVal = x;
        }
        cin >> x;
    }
    cout << minVal;
    return 0;
}
