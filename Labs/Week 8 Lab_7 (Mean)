#include <iostream>

using namespace std;

double x;

double sum = 0;
double avg;
int counter = 0;
int main() {
    cin >> x;
    while(x != -1){
        sum = sum + x;
        cin >> x;
        counter++;
    }
    avg = sum/counter;
    cout << avg;
    return 0.0;
}
